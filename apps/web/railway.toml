[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/web/Dockerfile"

[build.args]
VITE_API_URL = "https://api-v2-production-1048.up.railway.app/api/v1"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
