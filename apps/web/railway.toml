[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/web/Dockerfile"

[build.args]
VITE_API_URL = "https://api-production-b51d.up.railway.app/api/v1"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
