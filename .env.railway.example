# Railway Environment Variables Example
# Copy these to your Railway project's environment variables

# ===========================================
# API SERVICE ENVIRONMENT VARIABLES
# ===========================================

# Server
NODE_ENV=production
PORT=3001

# Database (Railway PostgreSQL - use Railway's DATABASE_URL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (Railway Redis - use Railway's REDIS_URL)
REDIS_URL=${{Redis.REDIS_URL}}

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secure-jwt-secret-minimum-32-characters

# Google OAuth (from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Gemini AI (from Google AI Studio)
GEMINI_API_KEY=your-gemini-api-key

# Stripe (from Stripe Dashboard)
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Twilio (from Twilio Console)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Storage (for production, use GCS)
# For Railway, you can use local storage initially
STORAGE_TYPE=local
# Or for Google Cloud Storage:
# STORAGE_TYPE=gcs
# GCS_BUCKET_NAME=your-bucket-name
# GCS_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Frontend URL (your Railway web service URL)
FRONTEND_URL=https://your-web-app.up.railway.app
CORS_ORIGINS=https://your-web-app.up.railway.app

# ===========================================
# WEB SERVICE ENVIRONMENT VARIABLES
# ===========================================

# API URL (your Railway API service URL - set as build arg)
VITE_API_URL=https://your-api.up.railway.app

# ===========================================
# RAILWAY SHARED VARIABLES
# ===========================================
# These are automatically provided by Railway when you add services:
# - DATABASE_URL (when you add PostgreSQL)
# - REDIS_URL (when you add Redis)
# - RAILWAY_STATIC_URL (your service URL)
